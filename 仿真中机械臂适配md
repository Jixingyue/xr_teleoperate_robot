# 如何添加自定义机械臂到仿真系统

## 文档概述

本文档详细说明了如何在 PyBullet 仿真系统中添加和配置自定义机械臂。通过分析 `teleop_hand_and_arm_sim.py` 和相关配置文件，本文档提供了完整的集成步骤和技术细节。

---

## 系统架构概述

### 核心组件

```
teleop_hand_and_arm_sim.py (主仿真程序)
    │
    ├── UniversalRobotArmIK (通用逆运动学解算器)
    │   └── 读取 config/robot_config.yml
    │
    ├── PyBulletSimSimpleSmooth (仿真环境)
    │   ├── 加载 URDF 模型
    │   ├── 关节控制
    │   └── 相机渲染
    │
    └── TeleVuerWrapper (XR遥操作接口)
```

### 工作流程

1. **初始化阶段**：加载机器人配置和URDF模型
2. **遥操作阶段**：获取XR设备输入数据（头部姿态、手臂位姿等）
3. **逆运动学求解**：将目标位姿转换为关节角度
4. **仿真执行**：控制机械臂关节移动到目标位置
5. **可视化反馈**：渲染双目相机图像返回给XR设备

---

## 添加自定义机械臂的步骤

### 步骤 1：准备 URDF 模型

#### 1.1 文件结构

将机械臂的URDF文件放置在 `assets/` 目录下：

```
assets/
  └── your_robot_description/
      ├── your_robot.urdf          # 主URDF文件
      └── meshes/                   # 
          ├── link1.stl
          ├── link2.stl
          └── ...
```

#### 1.2 URDF 要求

- **关节类型**：至少包含 6-7 个旋转关节（REVOLUTE）
- **末端执行器**：明确定义末端执行器连杆（end effector link）
- **夹爪（可选）**：如果有夹爪，需要定义夹爪关节
- **坐标系**：遵循标准的机器人坐标系约定（通常 Z 轴向上）

#### 1.3 URDF 示例结构

```xml
<robot name="your_robot">
  <!-- 基座 -->
  <link name="base_link">
    <visual>...</visual>
    <collision>...</collision>
  </link>
  
  <!-- 关节1 -->
  <joint name="joint1" type="revolute">
    <parent link="base_link"/>
    <child link="link1"/>
    <axis xyz="0 0 1"/>
    <limit lower="-3.14" upper="3.14" effort="100" velocity="1.0"/>
  </joint>
  
  <!-- 更多关节... -->
  
  <!-- 末端执行器 -->
  <link name="end_effector_link">
    <visual>...</visual>
  </link>
  
  <!-- 夹爪关节（可选） -->
  <joint name="gripper_joint" type="prismatic">
    <parent link="end_effector_link"/>
    <child link="gripper_finger"/>
    <limit lower="0" upper="0.04" effort="20" velocity="0.2"/>
  </joint>
</robot>
```

---

### 步骤 2：配置机器人参数

在 `config/robot_config.yml` 中添加机器人配置：

```yaml
YOUR_ROBOT_NAME:
  # URDF文件路径
  urdf_path: 'assets/your_robot_description/your_robot.urdf'
  urdf_dir: 'assets/your_robot_description/'
  urdf_path_test: 'assets/your_robot_description/your_robot.urdf'
  urdf_dir_test: 'assets/your_robot_description/'
  
  # 机械臂关节配置
  arm_joints:
    - name: 'joint1'       # 关节名称（必须与URDF中一致）
      index: 0             # 关节索引
    - name: 'joint2'
      index: 1
    - name: 'joint3'
      index: 2
    - name: 'joint4'
      index: 3
    - name: 'joint5'
      index: 4
    - name: 'joint6'
      index: 5
  
  # 夹爪关节配置（可选）
  gripper_joints:
    - name: 'gripper_left_joint'
      index: 7
    - name: 'gripper_right_joint'
      index: 8
  
  # 需要锁定的关节（如果有）
  joints_to_lock:
    - "gripper_left_joint"
    - "gripper_right_joint"
  
  # 末端执行器配置
  end_effectors:
    - name: 'R_ee'                    # 末端执行器名称
      end_joint: 'joint6'             # 最后一个关节名称
      end_link: 'end_effector_link'   # 末端执行器连杆名称
      offset: [0.05, 0, 0]            # 末端偏移量（米）
  
  # 手臂长度缩放配置
  arm_scaling:
    human_length: 0.65    # 人类手臂长度（米）
    robot_length: 0.75    # 机器人手臂长度（米）
  
  # 关节限制（弧度）
  joint_limits:
    - [-3.14159, 3.14159]   # joint1 限制
    - [-3.14159, 3.14159]   # joint2 限制
    - [-3.14159, 3.14159]   # joint3 限制
    - [-3.14159, 3.14159]   # joint4 限制
    - [-3.14159, 3.14159]   # joint5 限制
    - [-3.14159, 3.14159]   # joint6 限制
```

#### 配置说明

**arm_joints**：
- 定义机械臂的主要运动关节
- `index` 必须与 PyBullet 中的关节索引对应
- 顺序应从基座到末端

**gripper_joints**：
- 定义夹爪的控制关节
- 用于控制抓取动作
- 可以为空（如果没有夹爪）

**joints_to_lock**：
- 在逆运动学求解时需要锁定的关节
- 通常包括夹爪关节和其他不参与IK求解的关节

**end_effectors**：
- `offset`：从最后一个关节到实际末端执行器的偏移
- 用于精确控制末端位置

**arm_scaling**：
- `human_length`：操作者手臂长度（从肩到腕）
- `robot_length`：机器人手臂长度（从基座到末端）
- 系统会自动缩放目标位置以匹配机器人尺寸

**joint_limits**：
- 定义每个关节的运动范围
- 用于逆运动学求解时的约束
- 单位为弧度

---

### 步骤 3：修改仿真代码

在 `teleop_hand_and_arm_sim.py` 中修改以下部分：

#### 3.1 修改机器人类型

```python
# 第35行附近
self.arm_ik = ArmIK(robot_type='YOUR_ROBOT_NAME', unit_test=False, visualization=False)
```

#### 3.2 修改 URDF 加载路径

```python
# 第92行附近
try:
    arm_urdf = "assets/your_robot_description/your_robot.urdf"
    arm_pos = [0, 0, 0.6]  # 调整基座位置
    arm_orientation = p.getQuaternionFromEuler([0, 0, 0])
    self.arm_id = p.loadURDF(arm_urdf, arm_pos, arm_orientation, useFixedBase=True)
except:
    print("警告: 无法加载机械臂模型")
    raise Exception("无法加载机械臂模型")
```

#### 3.3 调整初始关节角度（可选）

```python
# 第153-156行附近
# 根据你的机器人调整初始姿态
self.init_joint_angles = np.deg2rad(np.array([0, -30, 120, 150, 80, 0]))   
for i, angle in zip(self.joint_indices, self.init_joint_angles):
    p.resetJointState(self.arm_id, i, angle)
```

#### 3.4 调整夹爪控制（如果有）

```python
# 第275-279行附近
if self.gripper_joint_indices is not None and len(self.gripper_joint_indices) > 0:
    # 根据你的夹爪类型调整控制逻辑
    joint_value = right_trigger / 100.0 / 2.0
    joint_value = 0.04 if joint_value >= 0.04 else joint_value
    for i, joint_idx in enumerate(self.gripper_joint_indices):
        p.resetJointState(self.arm_id, joint_idx, joint_value)
```

#### 3.5 修改 TeleVuerWrapper 配置

```python
# 第408行附近
tv_wrapper = TeleVuerWrapper(
    binocular=BINOCULAR, 
    use_hand_tracking=xr_mode == "hand", 
    img_shape=tv_img_shape, 
    img_shm_name=tv_img_shm.name, 
    return_state_data=True, 
    return_hand_rot_data=False,
    robot_type="YOUR_ROBOT_NAME"  # 修改为你的机器人名称
)
```

---

### 步骤 4：验证配置

#### 4.1 检查 URDF 加载

运行以下测试脚本验证 URDF 文件：

```python
import pybullet as p
import pybullet_data

# 连接 PyBullet
physics_client = p.connect(p.GUI)
p.setAdditionalSearchPath(pybullet_data.getDataPath())
p.setGravity(0, 0, -9.81)

# 加载地面
p.loadURDF("plane.urdf")

# 加载你的机器人
robot_id = p.loadURDF("assets/your_robot_description/your_robot.urdf", 
                      [0, 0, 0.5], 
                      useFixedBase=True)

# 打印关节信息
num_joints = p.getNumJoints(robot_id)
print(f"关节总数: {num_joints}")
for i in range(num_joints):
    joint_info = p.getJointInfo(robot_id, i)
    print(f"关节 {i}: {joint_info[1].decode('utf-8')}, 类型: {joint_info[2]}")

# 保持窗口打开
input("按Enter退出...")
p.disconnect()
```

#### 4.2 测试逆运动学

```python
from robot_control.universal_robot_arm_ik import UniversalRobotArmIK
import numpy as np

# 创建IK解算器
arm_ik = UniversalRobotArmIK(robot_type='YOUR_ROBOT_NAME', 
                             unit_test=True, 
                             visualization=True)

# 测试目标位姿
target_pose = np.eye(4)
target_pose[:3, 3] = [0.5, 0.0, 0.6]  # 目标位置

# 求解IK
sol_q, sol_tauff = arm_ik.solve_ik(right_wrist=target_pose)
print(f"关节角度解: {sol_q}")
```

---

## 关键技术细节

### 1. 逆运动学求解流程

```python
# teleop_hand_and_arm_sim.py 第218-258行
def solve_inverse_kinematics(self, right_pose, current_lr_arm_motor_q=None):
    """
    执行逆运动学计算
    
    流程：
    1. 接收目标位姿（4x4变换矩阵）
    2. 使用当前关节角度作为初始猜测
    3. 调用 arm_ik.solve_ik() 求解
    4. 检查关节角度是否在限制范围内
    5. 返回解算的关节角度
    """
    # 使用当前关节角度作为初始猜测，提高求解效率
    if current_lr_arm_motor_q is not None:
        sol_q, _ = self.arm_ik.solve_ik(right_wrist=right_pose, 
                                        current_lr_arm_motor_q=current_lr_arm_motor_q)
    else:
        sol_q, _ = self.arm_ik.solve_ik(right_wrist=right_pose)
    
    # 验证关节限制
    for i, (q, (min_limit, max_limit)) in enumerate(zip(sol_q, self.arm_ik.config['joint_limits'])):
        if q < min_limit or q > max_limit:
            sol_q[i] = np.clip(q, min_limit, max_limit)
    
    return sol_q
```

### 2. 关节控制机制

```python
# teleop_hand_and_arm_sim.py 第260-290行
def control_joints(self, target_angles, right_trigger):
    """
    控制机械臂关节和夹爪
    
    特点：
    1. 使用 POSITION_CONTROL 模式
    2. 自动等待关节到达目标位置（误差 < 0.5弧度）
    3. 最大等待60帧防止死锁
    """
    for i, angle in zip(self.joint_indices, target_angles):
        p.setJointMotorControl2(
            self.arm_id, 
            i, 
            p.POSITION_CONTROL, 
            targetPosition=angle,
            force=100.0,
            maxVelocity=30.0
        )
    
    # 等待关节稳定
    while True:
        p.stepSimulation()
        current_angles = self.get_current_state()['joint_angles']
        if np.allclose(target_angles, current_angles, atol=0.5):
            break
```

### 3. 坐标系转换

系统使用以下坐标系：

- **XR设备坐标系**：右手坐标系，Y轴向上
- **机器人坐标系**：Z轴向上
- **缩放因子**：`scale = robot_arm_length / human_arm_length`

```python
# universal_robot_arm_ik.py 第276-291行
def scale_arms(self, human_left_pose, human_right_pose, 
               human_arm_length=0.60, robot_arm_length=0.75):
    """
    缩放手臂位姿以匹配机器人尺寸
    """
    scale_factor = robot_arm_length / human_arm_length
    
    if human_right_pose is not None:
        robot_right_pose = human_right_pose.copy()
        robot_right_pose[:3, 3] *= scale_factor  # 只缩放位置，不缩放旋转
        return None, robot_right_pose
```

### 4. 性能优化

```python
# 相机渲染优化
self.camera_resolution = (640, 480)  # 降低分辨率
p.configureDebugVisualizer(p.COV_ENABLE_RGB_BUFFER_PREVIEW, 0)  # 禁用预览

# 使用硬件加速渲染器
renderer=p.ER_BULLET_HARDWARE_OPENGL

# 自适应帧率控制
if main_loop_time['latest'] < 16.0:
    time.sleep(0.016 - main_loop_time['latest'] / 1000.0)
```

---

## 常见问题和解决方案

### 问题 1：机械臂抖动或不稳定

**原因**：
- 逆运动学求解不收敛
- 关节限制设置不合理
- 目标位姿超出工作空间

**解决方案**：
```python
# 增加平滑滤波
self.smooth_filter = WeightedMovingFilter(
    np.array([0.4, 0.3, 0.2, 0.1]),  # 调整权重
    self.reduced_robot.model.nq
)

# 调整IK求解器参数
opts = {
    'ipopt': {
        'max_iter': 100,  # 增加迭代次数
        'tol': 1e-4       # 放宽容差
    }
}
```

### 问题 2：末端位置误差大

**原因**：
- `end_effector` 的 `offset` 配置不正确
- 手臂长度缩放比例不准确
- 基座高度设置错误

**解决方案**：
```python
# 1. 测量实际末端偏移
# 使用正运动学验证
state = simulator.get_current_state()
end_pos = state['end_position']
print(f"实际末端位置: {end_pos}")

# 2. 调整配置文件中的offset
end_effectors:
  - name: 'R_ee'
    end_joint: 'joint6'
    end_link: 'end_effector_link'
    offset: [0.08, 0, 0]  # 调整此值

# 3. 测量并更新手臂长度
arm_scaling:
  human_length: 0.65
  robot_length: 0.80  # 修改为实际测量值
```

### 问题 3：关节超出限制

**原因**：
- URDF 中的关节限制与配置文件不一致
- 目标位姿不可达

**解决方案**：
```python
# 1. 从URDF提取实际限制
for i in range(num_joints):
    joint_info = p.getJointInfo(robot_id, i)
    lower_limit = joint_info[8]
    upper_limit = joint_info[9]
    print(f"关节 {i}: [{lower_limit}, {upper_limit}]")

# 2. 更新配置文件
joint_limits:
  - [lower_limit, upper_limit]  # 使用实际值

# 3. 添加安全裕度
joint_limits:
  - [-3.0, 3.0]  # 比实际限制留出0.14弧度裕度
```

### 问题 4：夹爪不工作

**原因**：
- 夹爪关节索引错误
- 控制值范围不正确

**解决方案**：
```python
# 1. 验证夹爪关节索引
for i in range(num_joints):
    joint_info = p.getJointInfo(robot_id, i)
    if 'gripper' in joint_info[1].decode('utf-8').lower():
        print(f"夹爪关节: {i} - {joint_info[1].decode('utf-8')}")

# 2. 调整控制逻辑
# 对于棱镜关节（prismatic）
joint_value = right_trigger / 100.0 * max_gripper_opening

# 对于旋转关节（revolute）
joint_value = right_trigger / 100.0 * np.deg2rad(max_gripper_angle)
```

---

## 示例：添加 UR3e 机器人

完整示例，展示如何添加 Universal Robots UR3e：

### 1. 配置文件

```yaml
# config/robot_config.yml
UR3E:
  urdf_path: 'assets/ur_description/ur3e/ur3e.urdf'
  urdf_dir: 'assets/ur_description/ur3e/'
  urdf_path_test: 'assets/ur_description/ur3e/ur3e.urdf'
  urdf_dir_test: 'assets/ur_description/ur3e/'
  
  arm_joints:
    - name: 'shoulder_pan_joint'
      index: 0
    - name: 'shoulder_lift_joint'
      index: 1
    - name: 'elbow_joint'
      index: 2
    - name: 'wrist_1_joint'
      index: 3
    - name: 'wrist_2_joint'
      index: 4
    - name: 'wrist_3_joint'
      index: 5
  
  end_effectors:
    - name: 'R_ee'
      end_joint: 'wrist_3_joint'
      end_link: 'wrist_3_link'
      offset: [0.05, 0, 0]
  
  arm_scaling:
    human_length: 0.65
    robot_length: 0.75
  
  joint_limits:
    - [-6.28, 6.28]
    - [-6.28, 6.28]
    - [-3.14, 3.14]
    - [-6.28, 6.28]
    - [-6.28, 6.28]
    - [-6.28, 6.28]
```

### 2. 修改仿真代码

```python
# teleop_hand_and_arm_sim.py

# 第35行
self.arm_ik = ArmIK(robot_type='UR3E', unit_test=False, visualization=False)

# 第92行
arm_urdf = "assets/ur_description/ur3e/ur3e.urdf"

# 第408行
tv_wrapper = TeleVuerWrapper(
    # ... 其他参数 ...
    robot_type="UR3E"
)
```

### 3. 运行测试

```bash
cd /home/robot/unitree/xr_teleoperate_robot
python teleop/teleop_hand_and_arm_sim.py
```

---

## 参考资料

### 相关文件

- `teleop/teleop_hand_and_arm_sim.py` - 主仿真程序
- `teleop/robot_control/universal_robot_arm_ik.py` - 通用IK解算器
- `teleop/utils/config_loader.py` - 配置加载器
- `config/robot_config.yml` - 机器人配置文件

### 依赖库

- **PyBullet**: 物理仿真引擎
- **Pinocchio**: 机器人运动学库
- **CasADi**: 优化求解器
- **NumPy**: 数值计算

### 外部资源

- [PyBullet 文档](https://pybullet.org/)
- [Pinocchio 文档](https://stack-of-tasks.github.io/pinocchio/)
- [URDF 规范](http://wiki.ros.org/urdf/XML)

---

## 总结

添加自定义机械臂到仿真系统需要以下步骤：

1. ✅ 准备符合规范的 URDF 模型
2. ✅ 在配置文件中添加机器人参数
3. ✅ 修改仿真代码以使用新的机器人类型
4. ✅ 验证 URDF 加载和关节控制
5. ✅ 测试逆运动学求解
6. ✅ 调试和优化性能

通过遵循本文档的步骤，你可以快速集成任何6自由度或7自由度的机械臂到仿真系统中，并实现XR遥操作控制。

---



